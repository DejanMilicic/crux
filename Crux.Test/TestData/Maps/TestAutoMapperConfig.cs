namespace Crux.Test.TestData.Maps
{
    public static class TestAutoMapperConfig
    {
        //private static bool isUsed;

        //public static void RegisterProfiles()
        //{
        //    if (!isUsed)
        //    {
        //        Mapper.Initialize(
        //            cfg =>
        //            {
        //                cfg.AddProfile(new CoreProfile());
        //                cfg.AddProfile(new TestApiProfile());
        //                cfg.AddProfile(new TestMasterProfile());
        //                cfg.ValidateInlineMaps = false;
        //            });
        //        isUsed = true;
        //    }
        //}
    }
}