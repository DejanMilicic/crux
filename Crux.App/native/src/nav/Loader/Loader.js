import React from 'react';
import { Text } from 'react-native';

export function Loader() {
    return (
        <React.Fragment>
            <Text>Loading...</Text>
        </React.Fragment>
    );
}